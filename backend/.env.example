PORT=3001

# Postgres — this project's own bundled container (docker-compose.yml)
PGHOST=localhost
PGPORT=55432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=kdbs2

CORS_ORIGIN=http://localhost:5173
